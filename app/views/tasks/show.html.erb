<h2><%= @task.details %></h2>
<h3><%= check_box_tag '@tasks_ids[]', @task.id -%>This is completed</h3>
<%= link_to "Back to list" , tasks_path %>

<%= link_to 'edit',edit_task_path(@task.id) %>
<%= link_to "Delete", task_path(@task),
      method: :delete%>
